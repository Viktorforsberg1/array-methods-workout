//sort

//Metoden sort, sorterar elementen i en array, baserat på UTF-16, som är en slags teckenkodning.
//Lägst värde först. Returnerar resultaten i en ny array.

const letters = ['a', 'f', 'r', 'd', 'o'];

letters.sort();

console.log(letters); //[ 'a', 'd', 'f', 'o', 'r' ]